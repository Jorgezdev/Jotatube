# VITE_API_URL: URL base de la API para cuando se conecte un backend real.
# No se usa todavía (el proyecto corre 100% con datos mock + localStorage),
# queda declarada para la guía de integración con backend del README.
VITE_API_URL="http://localhost:5000/api"
